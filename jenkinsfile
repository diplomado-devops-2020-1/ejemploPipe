pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                bat './mvnw.cmd clean compile -e'
            }
        }

        stage('test') {
            steps {
                bat './mvnw.cmd compile -e'
            }
        }

        stage('test2') {
            steps {
                sh '''
                    /usr/local/opt/maven/bin/mvn test
                '''
            }
        }
    }
}
